library project libssl is
    for Languages use ("C");
    for Library_Name use "ssl";
    for Library_Kind use "dynamic";
    for Library_Dir use "lib";
    for Externally_Built use "true";
end libssl;
